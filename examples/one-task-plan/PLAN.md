# The smallest plan that is still a plan

**Size:** XS

One task. Copy this directory, change the two files, run the gate.
Below four tasks Grillin runs reduced — no separate adversary, no collision
step — so this is the whole ceremony, not a stripped-down version of it.

Use this when the work has to survive the conversation: the folder, the owner,
the status line and the re-runnable done-command are what let a fresh context
pick it up from disk, and the gate can read all four. When you are present for
the whole job and the artefact you get back is a report rather than a directory,
you want [`templates/BRIEF.md.template`](../../templates/BRIEF.md.template)
instead — same rails, no bookkeeping, and no gate.

| ID | Task | Owner | Blocked by |
|---|---|---|---|
| T1 | check the thing that keeps breaking | you | — |
