<!-- Lives at tasks/<ID>/TASK.md. This folder is your briefing and your output location:
     everything you need is here, everything you produce goes here. One folder per task. -->

# <ID> — <Short title>

**Type:** BUILD | REFACTOR | RECONSIDER | ASK | VERIFY | LOOP
**Status:** NOT STARTED | IN PROGRESS | BLOCKED | DONE — *update this line in place as you go;
it is the progress record, and it is the only one that survives a lost conversation*
**Agent:** `<persona>` · **Model:** <cheap | mid | top> · **Effort:** <default | high>
**Blocked by:** <ids> · **Blocks:** <ids>
**Mandatory downstream:** <adversarial passes — TWO, the second with fresh context, on money/authz? integrator?>

<!-- READER — only on the two reading roles. See OPERATING-THE-PLAN.md §7.
     **Reader:** adversary  → judges whether the RESULT is true. Its Owner must appear nowhere
                              else in the plan; the gate fails the plan if it does. It reads the
                              deliverable and its evidence, NOT the plan's reasoning. It reports,
                              it does not repair.
     **Reader:** health     → judges whether the RULES were followed. Contamination is expected
                              and required; that is exactly why it cannot also be the adversary. -->
**Reader:** <adversary | health — omit this line entirely on ordinary work tasks>

<!-- DELIVERY — required on any task whose Owner is a human, once it leaves NOT STARTED.
     A human does not read this directory, so record that the contract actually reached them,
     and freeze it. The hash covers "What you own", "Steps", "Done means" and "Do NOT".
     Change any of them afterwards and `check_frozen_human_contracts` fails the plan: grading
     someone against wording they were never given is moving the goalpost, not a deviation.
       printf '%s' "$(sed -n '/^## What you own/,$p' TASK.md)" | sha256sum   # see the script -->
**Delivered:** <YYYY-MM-DD> · contract sha256:<first 12 hex>

Rules: [`../_RULES.md`](../_RULES.md)

**Skills — invoke ALL of these before your first tool call, in this order:** `<skill>` · `<skill>`
They are your starting context. Reading them after you begin is worth a fraction of reading them
before.

**Wave:** <W#> · see [`../_WORKTREES.md`](../_WORKTREES.md)
**Branch:** `<prefix>/<ID>-<slug>` · **Worktree:** `<path>`

**Containment.** You may READ anywhere. You may WRITE only inside your worktree and this task
folder. `<live path>` is the **deployed checkout** — never write to it. If you notice a defect in
a file you do not own, record it in `FINDINGS.md`; do not fix it.

**Files you must NOT edit — a single integrator owns them:** `<contended file>` · `<contended file>`.
Emit fragments into this folder instead: `<FRAGMENT>.ext`.

**If reality diverges from this file, the file is wrong — say so.** A step that cannot be
performed as written, an owned path that does not exist, a "done" that cannot be evidenced: record
it in `QUESTIONS.md` and stop. Do not quietly do the adjacent thing. Amending the plan is the
integrator's call, not yours, but the divergence must be *reported* rather than absorbed.

**Commit** to your branch before reporting: code + `FINDINGS.md` + `CHANGES.md` + any fragments.
First line `<ID>: <what>`, body = three lines of why. Do NOT merge, do NOT run `git worktree`,
do NOT touch the main branch.

## Why this exists

<Two to five sentences. State the problem in terms of consequence, not activity. If this task
exists because something already went wrong, say what went wrong — it is the most useful
sentence in the file.>

## What you own

<Exact paths. One owner per file. Name explicitly what you do NOT own and who does.>

## Steps

1. <Read the inventory section that briefs you — cite it. Do not re-derive.>
2. <Ordered, specific, referencing real paths.>
3. <...>

## Loop

**Converge, cap <N>.** <Cycle: do → verify → fix → confirm the fix → re-verify.> Exit when
<condition someone else can check>. Hitting the cap is a **stop-and-report**, never a silent
partial.

**You do not confirm your own repair.** A fix you made is checked by someone who did not make it —
same rule as the work itself, one level down.

## Done means

<Evidence another person or agent can check. Captured output, a diff, a status code, a count —
never "it works".>

## Do NOT

- Do NOT <the specific trap for this task>.
- Do NOT <the pattern that looks helpful and is not>.
- Do NOT spawn sub-agents. *(or: sub-agents must run at `<tier>`)*
- Everything in `../_RULES.md` §<universal do-NOT list>.

## Outputs

`FINDINGS.md` · `CHANGES.md` (if code changed) · `QUESTIONS.md` (if blocked) · `<fragments>`

All of them written **into this folder** (`tasks/<ID>/`), never anywhere else. A worker that lost
its context recovers by re-reading this directory — so if it is not in here, it does not exist.
